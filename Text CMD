185.115.218.47

login
-  cd .ssh
-  ssh ubuntu@jasper_dev.go-ao.be -i leerlingendev.pem
-  cd /var/www/html
-  

Voorbeelden:
php script maken:
- sudo vim create_user.php
-  - <?php $username = isset($_GET["name"]) ? $_GET["name"] : null;
-  - if (is_null(ûsername)) {
-  -  ....

ls /var/log/apache2 (var/log is voor heel linux een map..)

tail - f /var/log/apache2/error.log (toont u het laatste van het bestand)

Ctrl+c (dan ga je uit u vorige file)

tail -f (je kijkt naar het einde en hij is live en ziet alles er live bijkomen)

more /var/log/apache2/... (laat je meer zien dan tail -f)

php commando: 
- php exec: 
-  -  -  -  -  -  exec(string $command, array &$output = null, int &$result_code = null): string|false
-  -  ls -ltr (sorteerd alles) 

escape + : w/q om te saven of te quitten
escape + i = (insert)

git:
ssh-keygen 


